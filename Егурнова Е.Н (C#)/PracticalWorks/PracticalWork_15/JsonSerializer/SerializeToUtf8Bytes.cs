namespace JsonSerializer
{
    internal class SerializeToUtf8Bytes
    {
        private string text;

        public SerializeToUtf8Bytes(string text)
        {
            this.text = text;
        }
    }
}